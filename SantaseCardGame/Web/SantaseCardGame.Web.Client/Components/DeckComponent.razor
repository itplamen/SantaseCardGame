@inject GameState GameState;

@if (Deck.Cards.Any())
{
    <CardComponent Card="Deck.TrumpCard" IsTrumpCard="true" OnClick="GameState.ChangeTrumpCard"></CardComponent>
    <div id="DeckCards" class="card-component" @onclick="GameState.CloseDeck">
        <div id="DeckCardsCount">@(Deck.Cards.Count)</div>
        <img src="/images/cards/Back.png" id="DeckBackCard" alt="DeckBackCard" class="card-component" />
    </div>
}

@code {
    [Parameter]
    public Deck Deck { get; set; }

    protected override void OnInitialized()
    {
        GameState.OnExchangeTrumpCardForNineOfTrumps += ExchangeTrumpCardForNineOfTrumps;
    }

    private void ExchangeTrumpCardForNineOfTrumps(Card nineOfTrumpsCard)
    {
        Deck.TrumpCard = nineOfTrumpsCard;

        StateHasChanged();
    }
}